# Dependencies
node_modules/

# Build output
dist/
*.tsbuildinfo

# Drizzle generated artifacts
# Note: migration SQL files ARE tracked (schema evolution history).
# Only the meta/ snapshots and journal are ignored if they cause churn,
# but for now we track the whole drizzle/ directory.
# (was: drizzle/ — changed to track migrations for team collaboration)

# Local agent analysis artifacts (never commit to product PRs)
.codebase-memory/
.learnings/
docs/superpowers/plans/
vite.review.config.ts

# Test output
coverage/
playwright-report/
test-results/
sbom-*.cdx.json
release-manifest.json

# Environment files
.env
.env.*
!.env.example

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Temporary
.tmp/
tmp/
temp/
backups/
