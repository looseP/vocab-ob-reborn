# 直接执行版 Prompt：L3 空间体验层设计与实现（一次外派到底）

> 用途：整段复制给执行 agent（新会话、无本对话上下文）。本版为**直接任务实现**：你一人完成「设计基线 → 视觉走查 → 分批实现 → 最终报告」，全程不停下来等评审；仅当触碰"已定决策冲突"时才中断上报。日期 2026-09-12。
> 项目根：`D:\Temp\Myawesomeapp\vocab-ob'`；代码根：`D:\Temp\Myawesomeapp\vocab-ob'\wt-main`（worktree，分支 main，HEAD=d3ae06b）。

---

## 0. 任务一句话

把 L3 空间从"功能能用"做到"开门有感"：替换 37 行占位首页为**素材宇宙**（最近导入 / 最近圈记 / 生长计数），补上**生长感**可视化，对齐阅读视图与圈记细节，并收敛全站视觉语言。这是设计稿里被整层漏掉的第 2 层，不是优化项。

**Why（必须理解）**：设计稿《核心对话导出/L1-L3分层词卡学习方案深度梳理.md》「空间三层拆解」第 2 层标题原文=「空间 = 一种体验（缺失，真正要设计的）」：书房入口=素材宇宙而非占位符；生长感=积累型情感价值（"用户看到自己的知识网络在长大"），不是消耗型。功能面（T01–T13c-2）已全部完成，用户体验不到差别的根因就是这一层缺失。实锤：`src/frontend/pages/L3HomePage.tsx` 全文 37 行，自述"挂载状态页"。

## 1. 必读材料（按序读完再动手）

1. `wt-main/docs/adr/0004` §6 → `0005` → `0008` → `0016` → `0017`–`0020`；
2. `核心对话导出/L1-L3分层词卡学习方案深度梳理.md`：§三层拆解、§书架形态决策（**选 b 平铺+语境计数**；0 数据不预装筛选器——"在年轻的书架上预装尴尬"）、Q7（高亮=**仅已圈偏移并集**，同形不匹配、未圈不亮）；
3. `_archive-2026-08/l3-context-space-requirements.md`（FR-1~FR-15，重点 FR-2 渲染层 / FR-3 圈记绑定 / FR-6 双向跳转）；
4. `_archive-2026-08/l3-context-space-player-design.md`（self-growing 构想）；
5. `deliverables/software-company/l3-upgrade-task-breakdown-2026-09-11.md` §0 外派通用约束**全文逐条继承**（门禁取证、并发写者、覆盖率纪律）；
6. 现状代码：`pages/L3HomePage.tsx`（替换对象）、`components/l3/L3ReadingView.tsx`（22.9KB：occurrence 并集高亮、句尾标号跳词卡面板、`?contextId=` 深链均已在，**复用不重写**）、`components/l3/L3Bookshelf.tsx`、`pages/L3*.tsx` 12 页、既有样式 token 体系（先盘点再用）。

**已知输入缺口（自行消化，不停工）**：无视觉稿（只有文字需求稿）→ 基线=你从文字稿提炼的理解，文档首页标注此性质；用户未点名痛点 → 按严重度全覆盖走查。

## 2. 执行流程（顺序做完，自行推进）

**Step 1｜设计基线（内部工件，仍需落盘）**：写 `wt-main/docs/design/l3-space/baseline.md`：体验目标、页面清单+信息层级、组件清单（复用 vs 新增）、状态矩阵（每页×空/加载/错/满，空态文案必须积累型导向）、视觉 token 映射（沿用既有 token，不新造体系）、动效与密度。**写完自查一遍与必读材料 2/3 的引用一致性，然后直接进 Step 2，不等任何人。**

**Step 2｜视觉走查**：起本地实例（Compose `web` 绑 `127.0.0.1:3001` 或 dev server），真实数据逐页截图，与基线对照出 P0/P1/P2 gap 清单（现状截图+基线条目引用+修法+S/M/L），落 `deliverables/software-company/l3-visual-audit-2026-09-12/`。此步是实现的派工单，做完直接派给自己。

**Step 3｜分批实现（每批独立提交、门禁全绿再进下一批）**：
- **B1 空间首页=素材宇宙**：替换占位页——最近导入、最近圈记、生长计数（条目数/边数随时间的积累呈现）；空态按状态矩阵；数据来源优先复用既有只读端点，确需新端点走完整 SOP（schema→contract `.strict()`→路由→operations.ts→api:openapi→client:generate→前端→tests/http/→api:governance）；
- **B2 生长感可视化**：选一个形态做深（生长计数趋势 / 网络密度变化 / 时间轴其一），不做三个半成品；
- **B3 阅读与圈记对齐**：按 FR-2/FR-3 核多词形高亮、搭配圈记、stub 建词、回流词详情；**Q7 冲突警示见 §3**；
- **B4 视觉收敛**：把 B1–B3 沉淀的密度/层级/空态/状态色/动效收敛为可复用件，L3 各页统一；L1/L2 页仅做低成本对齐，不重设计。

**Step 4｜流程加固**：`l3-upgrade-task-breakdown-2026-09-11.md` §0（或其后继模板）追加：凡 UI 卡必须引用设计基线条目+附前后截图对比，否则不予收口。

**Step 5｜最终报告**（见 §5）。

## 3. 唯一中断条件（触碰即停工上报）

发现以下任一情况，停止相关改动、记录证据、在最终报告（或即时）上报，**不得自行决策**：
- 实现意图与已定决策冲突——典型：想把阅读高亮从「仅已圈偏移并集」（Q7）扩为同形/多词形匹配；
- 需要改动 `drizzle-release/` 已应用迁移、需要动 L3 红线（零调度/只读消费）；
- 发现某 FR 实际已实现或需求稿与代码严重矛盾。

## 4. Non-goals

不改已应用迁移任何字节；不做音视频场景（FR-14）；不重构 T10–T12 功能逻辑；不在 `src/` 新建一级目录；不引入新 UI 框架/重依赖；不做 L1/L2 页面重设计。

## 5. 验收与交付

**工程门禁（每批提交前全绿）**：`npm run typecheck`、`arch:check`、`test:unit`、触及前端 `frontend:build`；触及 API 走 `api:governance`（本机显式 `API_CONTRACT_BASE_REF=<base sha>`，无 `origin/main`）；PASS 必须在能行使该分支的 base 上取证（继承 §0 取证纪律）。纯前端用 `--coverage.include="src/frontend/..."` 自报补充度量。

**体验交付（每张 UI 卡）**：基线条目引用 + 前后截图对比 + 空/加载/错/满四态截图，缺一视为未完成。

**最终报告**含：完成度矩阵（B1–B4+基线+走查 × 完成/部分/未完成）、门禁证据（命令+base ref+要点输出）、截图索引、偏离与决策记录、残留 gap、给用户的后续建议。**完整性门槛：部分完成就如实写部分，不得把局部说成整体；区分【本地跑】与【CI 绿】。**

## 6. 红线（违反视为未完成）

- git：可能有并发会话——逐文件点名暂存，**绝不 `git add -A`**；提交前 `git diff --cached --name-only` 复核；提交后 `git log -1` 验证；本机 `git rebase` 不可用；`update-ref` 只用扁平名。
- 路径含单引号：`V="/d/Temp/Myawesomeapp/vocab-ob'"`，先回显验证；认准 `wt-main`，glob 可能取到无引号同名目录。
- bash PATH 偶发失效 → PowerShell + 托管 node 直调（`C:\Users\20564\.workbuddy\binaries\node\versions\22.22.2-3\node.exe`）；输出 `Out-File -Encoding utf8` 落盘再读。
- 同文件多处编辑：整文件一次 Write 或 python 原子改（读→改→临时文件→os.replace），改后 grep 复核；不同消息批量 Edit 同文件会丢更新。
- 触及共享文件（`operations.ts`、契约、`openapi.json` 等 §0 清单）→ 串行或影子树验证。
