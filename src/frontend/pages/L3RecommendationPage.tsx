export function L3RecommendationPage() {
  return (
    <section className="l3-page">
      <p className="eyebrow">Recommendation Placeholder</p>
      <h2>Accepting link_gap creates a proposal bridge, not an active link.</h2>
      <p className="lede">
        Phase 4D should render recommendation queues and accept/reject actions while preserving proposal review before active L3 writes.
      </p>
      <div className="placeholder-panel">
        <strong>Accepted recommendation is not active evidence.</strong>
        <span>Confirm the generated proposal before showing the link as active.</span>
      </div>
    </section>
  );
}
