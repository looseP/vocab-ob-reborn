# Windows Docker Desktop local mode: copy to an untracked .env, replace every placeholder.
# Store the resulting .env outside OneDrive or other sync folders; do not commit it.
# All application images must be immutable digests.
APP_IMAGE=ghcr.io/OWNER/vocab-observatory-runtime@sha256:REPLACE_WITH_64_HEX_DIGEST
MIGRATION_IMAGE=ghcr.io/OWNER/vocab-observatory-migration@sha256:REPLACE_WITH_64_HEX_DIGEST
BACKUP_IMAGE=ghcr.io/OWNER/vocab-observatory-backup@sha256:REPLACE_WITH_64_HEX_DIGEST
# Pin Caddy to the digest resolved for the reviewed 2.10.2-alpine release.
CADDY_IMAGE=caddy@sha256:REPLACE_WITH_64_HEX_DIGEST
POSTGRES_IMAGE=postgres@sha256:REPLACE_WITH_64_HEX_DIGEST

# Local-only HTTPS endpoint. The Compose ports below are bound to loopback only.
# Caddy issues an internal certificate for localhost; trust its root certificate on Windows
# before using the browser. Do not change the bind address to 0.0.0.0 without entering
# the separately reviewed LAN/public deployment phase.
CADDY_EMAIL=local@example.invalid
CADDY_SITE_ADDRESS=localhost
APP_ORIGIN=https://localhost
CADDY_HTTP_BIND_ADDRESS=127.0.0.1
CADDY_HTTPS_BIND_ADDRESS=127.0.0.1
# Absolute Windows path with forward slashes. Docker Desktop must have access to the drive.
CADDY_CONFIG_FILE=D:/vocab-observatory/app/Caddyfile

# Generate unique random values. Do not reuse development defaults.
POSTGRES_USER=vocab
POSTGRES_PASSWORD=REPLACE_WITH_LONG_RANDOM_DATABASE_PASSWORD
POSTGRES_DB=vocab
OWNER_API_TOKEN=REPLACE_WITH_AT_LEAST_24_RANDOM_CHARACTERS
METRICS_BEARER_TOKEN=REPLACE_WITH_A_DISTINCT_24_RANDOM_CHARACTERS
LOCAL_OWNER_ID=00000000-0000-4000-8000-000000000001
BACKUP_SIGNING_KEY=REPLACE_WITH_LONG_RANDOM_SIGNING_KEY

# Must be an absolute Windows host path using forward slashes. Keep it outside the repo
# and any sync folder; Docker Desktop must have access to its drive.
BACKUP_HOST_DIR=D:/vocab-observatory/backups
BACKUP_RUNTIME_USER=node
BACKUP_INTERVAL_MS=86400000
BACKUP_RETENTION_COUNT=14
DB_POOL_MAX=5
LOG_LEVEL=info
READINESS_TIMEOUT_MS=500
SHUTDOWN_GRACE_MS=25000
LOGIN_RATE_LIMIT_WINDOW_MS=60000
LOGIN_RATE_LIMIT_ATTEMPTS=8
OUTBOX_POLL_INTERVAL_MS=1000
OUTBOX_BATCH_SIZE=20
OUTBOX_LEASE_SECONDS=60
OUTBOX_SHUTDOWN_GRACE_SECONDS=75
OUTBOX_STOP_GRACE_PERIOD=75s
LLM_REAPER_POLL_INTERVAL_MS=5000
LLM_REAPER_BATCH_SIZE=100

# Optional external enrichment. Keep blank to disable.
LLM_PROVIDER=
LLM_MODEL=
LLM_API_KEY=
LLM_BASE_URL=
DATAMUSE_ENABLED=false
